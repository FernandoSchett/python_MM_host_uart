# py_matrix_host

Host Python do acelerador.

Uso principal:

```powershell
python main.py generate --n 128 --output-dir matrix
python main.py uart --dry-run --input matrix/matrix_inputs.txt --expected matrix/matrix_expected.txt
```
